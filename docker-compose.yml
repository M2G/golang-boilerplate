version: '3.5'

services:
  api:
    build:
      context: .
      target: dev
    container_name: golang_starter
    volumes:
      - ./:/go/src/github.com/golang-starter
    env_file:
      - configuration/env/dev.env
    ports:
      - 8181:8181

